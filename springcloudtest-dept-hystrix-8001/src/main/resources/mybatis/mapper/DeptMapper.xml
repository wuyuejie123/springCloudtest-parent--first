<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.example_hystrix.dao.DeptMapper">

    <insert id="addDept" parameterType="org.example.entity.Dept">
        insert into dept(dname,db_source) values(#{dname},DATABASE());
    </insert>

    <select id="findById" resultType="org.example.entity.Dept" parameterType="java.lang.Long">
        select * from dept where deptno = #{deptno}
    </select>

    <select id="findAll" resultType="org.example.entity.Dept" >
        select * from dept
    </select>

</mapper>